# Vidmaker3 Bot Lightweight Requirements
# Only Discord bot logic - no heavy ML/video processing
# Build with: docker build -f Dockerfile.bot-lightweight -t vidmaker3-bot .

# Core Discord
discord.py>=2.3.0

# Web Framework (for health checks and potential API endpoints)
aiohttp>=3.9.0
fastapi>=0.109.0
uvicorn>=0.27.0

# Job Queue & Redis (optional - for async job processing)
redis>=5.0.0
rq>=1.16.0

# Configuration
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Google APIs (for Sheets - lightweight)
google-api-python-client>=2.125.0
google-auth>=2.25.0
gspread>=6.0.0

# Cloud Storage - R2 (boto3 for uploads)
boto3>=1.34.0
botocore>=1.34.0

# AI Image Generation (Together API)
together>=1.0.0

# OpenAI (for Twitter cog and other AI features)
openai>=1.0.0

# Utilities
requests>=2.31.0
python-json-logger>=2.0.0
structlog>=24.1.0

# Web scraping
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Logging
PyYAML>=6.0.0
